import { FRAME as Frame } from '../../../contracts/Figma';
import { RadiusTokens } from '../../../contracts/Tokens';
export declare function makeRadiusTokens(radiusFrame: Frame, remSize: number): RadiusTokens;
