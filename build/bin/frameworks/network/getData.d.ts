import { FigmaData } from '../../contracts/FigmaData';
export declare function getData(recompileLocal: boolean, figmagicFolder: string, figmaData: string, token: string | null, url: string | null): Promise<FigmaData>;
