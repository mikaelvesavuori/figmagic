language: node_js

node_js:
  - node

os:
  - linux
  - osx

script:
  - npm run setup
  - figmagic
  - \[ -d figma \]
  - \[ -f figma/figma.json \]
  - \[ -d tokens \]

notifications:
  email: false
